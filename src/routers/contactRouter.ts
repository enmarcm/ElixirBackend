import { Router } from "express";

const contactRouter = Router();

// contactRouter.get("/getAllContacts", (req, res) => {});

// contactRouter.post("/addContact", (req, res) => {});

export default contactRouter;
