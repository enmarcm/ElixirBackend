export enum Routes {
  MAIN = "/",
  PRODUCTS = "/products",
  SALES = "/sales",
  CLIENTS = "/clients",
  PAYMENT_METHODS = "/payment-methods",
  DELIVERY_METHODS = "/delivery-methods",
  DASHBOARD = "/dashboard",
}
